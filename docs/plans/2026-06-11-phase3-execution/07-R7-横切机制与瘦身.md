# R7 | 横切机制与瘦身（预计 1 天）

- 前置：R5（资产补全后再瘦身，避免同文件反复改）；R2 已做掉审计 E-5 的 `.light/` scaffold 部分。
- 背景：审计三章 E 组（横切机制 7 项）+ F 组（渐进披露瘦身 10 项）+ B 组（阈值统一 4 项，审计排在 R1 前后均可，归入本轮一次清完）+ I-4（agent-browser 依赖声明）。
- **开工第一步**：量基线——`wc -l` 统计 11 个常驻技能 SKILL.md 总行数，记入 PROGRESS 基线区（验收要 -15%）。

## B 组 · 阈值与立场统一（4 项，半天内）

| # | 冲突 | 修法 | 验收 |
|---|---|---|---|
| R7.1 | 检索库数：m03 撞车检查"≥2 库交叉" vs m04 复核"≥1 库"（复核者弱于自报者） | 统一 ≥2，m04 改 | grep 两技能口径一致 |
| R7.2 | 种子数：m05 "≥3~5" vs m06 必查"≥5" | 统一为"≥5；算力受限 ≥3 且须在 m06 报告显式标注" | 两技能同句式 |
| R7.3 | uv vs Poetry：a03 推 uv、a06 scaffold 只有 `--poetry` | a06 加 `--uv` 并设默认，Poetry 降备选；scaffold selftest 覆盖 | scaffold 两路径都过 |
| R7.4 | vaex 过时推荐（2023 后停维护） | `a09 decision_matrix.md:13` 换 DuckDB/polars streaming（表内 DuckDB 行已存在，顺带消自相矛盾） | grep vaex 仅余"已淘汰"标注 |

## E 组 · 横切机制升级（7 项）

### R7.5（E1）a08 self-review 分级执行档
- 现状："任何产出交付前过全清单"对轻任务（改一句摘要）= 11 项全跑，实际要么慢要么整体跳过失守。
- 修法：定义两档——**轻任务最小三项**（证据/事实/夸大）+ **重产出全量档**；档位判据写清（产出 ≥1 节/有数字结论/对外交付 → 全量）；orchestrator checkpoints.md 引用档位。
- 验收：两档判据可执行；orchestrator 引用到位。

### R7.6（E2）a07 变更广播挂接 passport
- 现状："定义修改立即回扫所有已产出材料"无机制支撑（已产出材料清单在哪？）。
- 修法：a07 的回扫流程明确挂接 `.light/passport.yaml` 的 artifacts 列表（orchestrator §4 维护的台账）作为"已产出材料"权威来源；无 passport 的轻项目退回 db09 version_history。
- 验收：a07/orchestrator 双向指认。

### R7.7（E3）a01 file-reading 三处
- 修法：① 补"宿主原生多模态优先"决策链（Claude Code 的 Read 可直读 PDF/图片，先原生后 pdfplumber，别教 agent 一律绕远路）；② 补视频一节（ffmpeg 抽帧 + 转写工具链——提示词明确要求，目前真空；给最小命令组）；③ 700 字工具杂烩段下沉 references，正文留决策树。
- 验收：正文行数下降且三能力齐。

### R7.8（E4）a02 Hermes 节降权
- 修法：a02 SKILL.md 的"Hermes 会话链恢复"（正文第一节，单一客户端内容）降为 references 一行指针（`references/hermes_session_lineage_recovery.md` 已存在）；注意与 R2 新增的会话衔接节排版协调——衔接协议是通用能力放前，Hermes 是特定客户端细节下沉。
- 验收：a02 正文更短且通用内容前置。

### R7.9（E5 余项）orchestrator pipeline 并行声明
- 修法：pipelines.md 声明可并行步骤（图表 m11 与引用 m10 实际可并行；m16 PPT 与 m12 排版可并行）；`.light/` scaffold 部分 R2 已做，**勿重复**。
- 验收：≥2 条链路标注并行段。

### R7.10（E6）m14 rubric 选用指引 + 字数预算
- 修法：三套评审 rubric（NeurIPS 官方/ScholarEval 8 维/GRADE）补选用指引——按论文类型择一不叠加（ML 会议→NeurIPS 套；系统综述→GRADE；通用→ScholarEval）；补 rebuttal 字数预算检查（会议常 1 页限制；纯 stdlib 数字数，可并入现有脚本或 ≤20 行新脚本）。
- 验收：选用表落地；字数检查可跑。

### R7.11（E7）m08→m14 findings 接口闭环
- 现状：m08 findings_schema 声称喂 m14/db09，消费侧无约定。
- 修法：m14 补"消费 m08 findings JSON"说明（哪些字段映射到审稿意见分类）；或 m08 删承诺——**优先补消费**（接口已设计好，补半边成本低）。
- 验收：双向 grep 可见。

### R7.12（I-4）a09 浏览器自动化依赖声明
- 修法：a09 工具选择加一行："投稿系统网页操作可装 agent-browser 类技能（438K 装），Light 不自建"；安装量数字带 last_checked。

## F 组 · 渐进披露瘦身（10 项）

> 统一手法：SKILL.md 正文只留"何时用+怎么决策+一行指针"，细节下沉 references/examples。每改一个跑 `check_skills.py + check_entry_docs.py`（description 改动会影响路由）。

| # | 对象 | 动作 |
|---|---|---|
| R7.13 | m15 SKILL.md:35-41 | API 参数整段与 references 重复——只留一行表+指针 |
| R7.14 | m16 SKILL.md:35 | 论文转稿 300+ 字段缩两行指针（references 已有同内容；R6 改动处注意合并） |
| R7.15 | m08 SKILL.md | 示例占全文 60%——下沉 examples/ 留一例 |
| R7.16 | a03 | "资产清单防漂移/脚本自测治理"两段维护元规则移 references |
| R7.17 | m07 description | 近 300 字砍到"能力+触发场景"（注意 ROUTER_EXAMPLES 回归——砍完跑路由样例核对） |
| R7.18 | a09 | 任务→工具映射瘦身 1/3 指向 matrix；skills.sh 安装量补 last_checked |
| R7.19 | a05 | ecosystem-2026.md 中文化；与 references.md 双时间锚声明以哪份为准；补 Style Dictionary/Terrazzo 一行（衔接 db05 tokens） |
| R7.20 | m17 SKILL.md | 三段 200-400 字长难段拆 bullet；补 CUMCM 2024 起 AI 使用规定（现仅有 MCM 的；执行时官网核实留痕） |
| R7.21 | a07 SKILL.md | 移除"比 mermaid-syntax-skill 能力维度更高"自证段（对执行 agent 是噪声） |
| R7.22 | m12 | 五个 .tex 骨架补编译验证记录（本机无 TeX 则标注"待 CI/有TeX环境验证"，不假装验过） |

## 验收（全轮）

- [ ] 常驻 11 技能 SKILL.md 总行数较基线 **-15%**（PROGRESS 记录前后数字）。
- [ ] B 组 4 处口径冲突清零（grep 留证）。
- [ ] a08 两档 + orchestrator 引用；a07 挂接 passport。
- [ ] 所有 description 改动后 ROUTER_EXAMPLES 样例人工回归一遍（重点 m07/m15/m16）。
- [ ] 门禁全绿。

## 提交切分建议

- `跨技能阈值:统一检索库数、种子数与uv默认`
- `自审与一致性技能:分级执行档并挂接产物台账`
- `文件读取技能:原生多模态优先与视频工具链`
- `常驻技能瘦身:细节下沉references与description精简`
- `返修技能:rubric选用指引与findings消费接口`

## 收尾

总纲 §5 八步；PROGRESS 更新（含行数前后对比）；打印 R8 启动提示词。
